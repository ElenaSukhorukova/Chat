<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-lg-0">
        <li class="nav-item">
          <%= link_to t('global.menu.chatroom'), root_path, class: "nav-link", 'aria-current': "page" %>
        </li>
        <li class="nav-item">
          <%= link_to t('global.menu.messages'), '#', class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= t('global.menu.account') %>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to t('global.menu.log_out'), '#', class: "dropdown-item" %>
            </li>
            <li>
              <%= link_to t('global.menu.log_in'), '#', class: "dropdown-item" %>
            </li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <%= link_to t('global.menu.sign_up'), '#', class: "dropdown-item" %>
            </li>
          </ul>
        </li>
        <li class="nav-item dropdown">
          <%= link_to '#', class: 'nav-link dropdown-toggle', data: {'bs-toggle': 'dropdown'}, 
            'aria-expanded': "false" do %>
            <%= tag.div '', class: "flag #{I18n.locale}-flag" %>
            <%= t I18n.locale %>
          <% end %>
          <ul class="dropdown-menu">
            <% I18n.available_locales.each do |locale| %>
              <li>
                <% if I18n.locale.to_s == locale %>
                  <%= tag.span t(locale), class: 'dropdown-item'%>
                <% else %>
                  <%= link_to t(locale), url_for(params_plus(locale: locale)),
                      class: 'dropdown-item' %>
                <% end %>
              </li>
              <li><hr class="dropdown-divider"></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>